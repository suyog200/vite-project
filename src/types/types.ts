export interface Location {
  id: number;
  title: string;
  city: string;
  state: string;
  country: string;
  zip: string;
};

export interface Department {
  id: number;
  title: string;
}

export interface Function {
  id: number;
  title: string;
}